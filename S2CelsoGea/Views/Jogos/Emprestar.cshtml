@model S2CelsoGea.Context.ContextModels.Jogo

@{
    ViewBag.Title = "Emprestar";
}

<h2>Emprestar</h2>


@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>Jogo</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.HiddenFor(model => model.Id)

        <div class="form-group">
            @Html.LabelFor(model => model.Name, htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.Name, new { htmlAttributes = new { @class = "form-control" } })
                @Html.ValidationMessageFor(model => model.Name, "", new { @class = "text-danger" })
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.WithUser_Id, "WithUser_Id", htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
               @Html.DropDownListFor(r => r.WithUser_Id, new SelectList(ViewBag.Users, "Id", "UserName"), "Selecione...", new { @class = "form-control input-sm" })
                @Html.ValidationMessageFor(model => model.WithUser_Id, "", new { @class = "text-danger" })
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Confirmar" onclick="" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {

    @*<script>

        var urlPost = '@Url.Action("Emprestar", "Jogos")';

        function post(e) {
            e.preventDefault()
            $.post(urlPost, $('form').serializeObject(), function (r) {
                bootbox.alert("Jogo emprestado com sucesso!")
            })
        }

    </script>*@

}
